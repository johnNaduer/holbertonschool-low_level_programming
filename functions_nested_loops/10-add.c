#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 * @x: first number
 * @y: second number
 * Return: Add of number one and number two.
 */
int add(int x, int y)
{

	return (x + y);
}
