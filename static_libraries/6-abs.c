#include "main.h"
/**
 * _abs - computes the absolute value of number
 *@x: is the input as an integer
 * Return: Always x or -x.
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (x * -1);
	}

}
